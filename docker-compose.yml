services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: tsure
      POSTGRES_USER: tsure
      POSTGRES_PASSWORD: tsure
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
